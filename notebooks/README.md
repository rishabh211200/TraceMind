# TraceMind Jupyter Notebooks

This directory contains exploratory data analysis (EDA), feature engineering experiments, and visualization prototyping notebooks.
Production code should always be formalized into `packages/` and `apps/`.
